const mapRecusive = require('./../dist/index.js');

describe('module syntax', () => {
  it('should have default export function', () => {
    expect(mapRecusive).toBeDefined();
  });
});

describe('module functionality', () => {

});
